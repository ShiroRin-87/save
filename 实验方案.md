# Bing Search + 结构化抽取与反向验证：联网大模型幻觉缓解方案

---

## 一、核心设定（不可变）

| 项目 | 固定值 |
|------|--------|
| 搜索工具 | **Bing Search API** |
| 基座模型 | **Gemini 3.0 Flash Preview**（全部实验环节统一使用） |
| 数据集 | **FreshQA** |
| 搜索结果 | **所有方法共享同一份缓存**，确保同一问题 → 同一搜索结果，严格公平 |
| 查询策略 | **所有方法使用同一组查询**，不涉及多查询/查询改写 |
| 实验方法 | **结构化抽取 + 反向验证**（仅此两个模块，不含跨源验证等） |
| Baseline | **Vanilla RAG**、**Self-RAG** |
| 消融实验 | **仅移除反向验证**（Vanilla RAG 作为全部移除的参照） |

---

## 二、方法概览

### 2.1 核心思想

在"搜索"和"生成"之间插入**结构化知识表**作为中间表示，解耦"理解"和"表达"。生成回答后，再对每个带引用的声明做**反向验证**，检查原文是否真的包含该声明。

### 2.2 方法流程（SEVE — Structured Extraction & Verification）

```
用户问题
   │
   ▼
┌──────────────────────────────────────────────┐
│  Step 1: Bing 检索                                   │
│  - 输入用户问题，调用 Bing Search API                 │
│  - 获取 Top-K 搜索结果（K 建议=5）             │
│  - 保存原始搜索结果到缓存文件                  │
└──────────────┬───────────────────────────────┘
               ▼
┌──────────────────────────────────────────────┐
│  Step 2: 结构化抽取（Structured Extraction）   │
│  - 从原始搜索结果中逐条提取事实声明            │
│  - 输出格式：{声明ID, 声明内容, 原文片段,       │
│    来源URL}                                    │
│  - 核心原则：只抽取，不推理、不跨源合成        │
└──────────────┬───────────────────────────────┘
               ▼
┌──────────────────────────────────────────────┐
│  Step 3: 校准生成（Calibrated Generation）     │
│  - 基于结构化知识表生成最终回答                │
│  - 每句声明附带引用标注                        │
│  - 无支撑声明自动降级为"[推测]"               │
│  - 信息不足时主动标注"[信息不足]"              │
└──────────────┬───────────────────────────────┘
               ▼
┌──────────────────────────────────────────────┐
│  Step 4: 反向验证（Reverse Verification）      │
│  - 对每个带引用的声明，反查原始搜索结果         │
│  - 判断：YES / PARTIAL / NO                   │
│  - 验证失败的声明标记删除或修正                │
│  - 生成最终验证报告                            │
└──────────────┬───────────────────────────────┘
               ▼
          最终输出
  （带引用 + 置信度标注 + 验证状态）
```

---

## 三、详细操作流程（可直接按步骤编码）

### 阶段 0：环境准备

```python
# 需要安装的库
pip install google-genai  # Gemini API SDK
pip install pandas
pip install jsonlines
```

**需要准备的配置：**

- `GEMINI_API_KEY`：Gemini 3.0 Flash Preview 的 API 密钥（通过 Google AI Studio 获取）
- `BING_API_KEY`：Bing Search API 密钥（通过 Azure Portal 获取）

---

### 阶段 1：FreshQA 数据集准备

**操作步骤：**

1. 从 [FreshQA 官方仓库](https://github.com/freshllms/freshqa) 下载数据集
2. 数据集包含约 600 个问题，每个问题标注了：问题文本、参考答案、时效性要求、是否可回答
3. 将数据转为统一的 JSON 格式：

```json
[
  {
    "question_id": "FQ001",
    "question": "Who is the current CEO of Twitter?",
    "reference_answer": "...",
    "requires_fresh": true,
    "is_answerable": true
  }
]
```

4. 存储为 `freshqa_questions.json`

**产物文件：** `data/freshqa_questions.json`

---

### 阶段 2：搜索结果预缓存（关键步骤！！！）

**目的：** 所有方法使用完全相同的搜索结果，消除搜索 API 返回结果随机性对方法对比的干扰。

**操作步骤：**

1. 遍历 `freshqa_questions.json` 中的每个问题
2. 调用 Bing Search API，传入原始问题文本
3. 保存每次搜索的完整返回结果

```json
// data/search_cache.json
{
  "FQ001": {
    "query": "Who is the current CEO of Twitter?",
    "timestamp": "2026-05-21T10:00:00Z",
    "results": [
      {
        "rank": 1,
        "title": "...",
        "url": "...",
        "snippet": "...",
        "full_text": "...",
        "publish_date": "..."
      },
      {
        "rank": 2,
        ...
      }
    ]
  }
}
```

4. 每个问题缓存 Top-5 结果（包含原文全文）
5. **所有后续方法严格从该缓存读取搜索结果**

**产物文件：** `data/search_cache.json`

**此步骤的作用：**
- 确保所有对比方法看到的搜索结果完全一致
- 论文中方法论部分声明："All compared methods share identical search results to isolate the effect of generation strategies."

**关于搜索结果的说明：**
- Bing Search API 返回的每条结果包含 `title`、`url`、`snippet`（约 160 字符摘要），不包含网页全文
- 所有方法均基于 snippet 进行生成和验证，这是合理且常见的研究设定（与 FreshQA 原论文、ALCE 等一致）
- 由于 Bing API 按调用次数计费，600 次搜索约 $1.8（$3/1000 次）

---

### 阶段 3：Baseline 方法实现

#### 3.1 Vanilla RAG

```
流程：
  Step A: 从 search_cache.json 读取该问题的 Top-5 搜索结果
  Step B: 拼接所有搜索结果的 full_text
  Step C: 将 {问题 + 拼接文本} 送入 Gemini 3.0 Flash Preview 生成答案
  Step D: 保存输出

Prompt 模板：
  你是一个问答助手。请基于以下搜索结果为用户问题提供准确、简洁的回答。
  每个事实声明后标注引用编号，如 [1][2]，对应搜索结果的序号。
  
  搜索结果：
  [1] {result_1_text}
  [2] {result_2_text}
  ...
  
  用户问题：{question}
  
  请回答：
```

**产物文件：** `outputs/vanilla_rag/answers.json`

---

#### 3.2 Self-RAG（Prompt-based Approximation）

> **方法定位说明：** 原版 Self-RAG（Asai et al., 2023）通过指令微调训练模型，使其在推理时自发输出 `<RELEVANT>`、`<SUPPORT>` 等特殊控制 token。由于本实验基于通用 API 模型（Gemini 3.0 Flash Preview），无法对模型做微调，因此采用 **prompt-based 近似复现**：遵循原论文的"生成→反思→精炼"三轮结构，但通过 prompt 指令（而非模型自发的反射 token）实现。论文中将以 "Prompt-based Self-RAG" 或 "Self-RAG (prompted)" 标注此 baseline，并在 Discussion 中作为 validity threat 讨论。

```
流程：
  Step A: 从 search_cache.json 读取该问题的 Top-5 搜索结果
  Step B: 拼接所有搜索结果的 full_text
  Step C: Self-RAG 三轮反思生成（严格按照 Self-RAG 论文）：

    Round 1 — 生成初步回答：
      基于搜索结果生成初始回答，同时为每个片段标注 [Relevant] / [Irrelevant]
    
    Round 2 — 自我反思：
      对每个声明做事实校验：
      - [Supported]: 搜索结果明确支撑
      - [Partially]: 搜索结果部分支撑
      - [Unsupported]: 搜索结果不支撑，标记删除
    
    Round 3 — 精炼生成：
      基于反思结果重新生成最终答案，只保留 [Supported] + [Partially] 的声明

  Step D: 输出最终答案（Self-RAG 论文中的最终输出格式）

Prompt 模板：

  Round 1:
    基于以下搜索结果回答问题。在回答中，为每个事实片段标注 [Relevant] 或 [Irrelevant]。
    
    搜索结果：{search_texts}
    问题：{question}

  Round 2:
    以下是一个回答及其标注。请检查每个声明是否被原文搜索结果支撑。
    标注每个声明为 [Supported]、[Partially]、[Unsupported]。
    
    回答：{round1_answer}
    搜索结果：{search_texts}

  Round 3:
    基于反思结果重新生成最终答案。只保留 [Supported] 和 [Partially] 的声明。
    
    原始回答：{round1_answer}
    反思结果：{round2_reflection}
    用户问题：{question}
```

**产物文件：** `outputs/self_rag/answers.json`

---

### 阶段 4：SEVE 方法实现（本文方法）

#### Step 4.1：结构化抽取

```
输入：
  - 从 search_cache.json 读取该问题的 Top-5 搜索结果

Prompt 模板（结构化抽取）：

  你是一个事实抽取器（不是推理器、不是作家）。你的唯一任务是从搜索结果中提取事实声明。
  
  规则：
  1. 只提取原文中明确陈述的事实，不推断、不总结、不跨源合成
  2. 每个声明一行，格式必须严格遵循：
     序号 | 提炼声明 | 原文片段 | 出处(URL)
  3. "提炼声明"是你从原文中提取的事实表述
     "原文片段"是搜索结果中支撑该声明的原始文本（必须逐字复制，不可改写）
  4. 如果原文没有明确说，不要填写
  5. 如果多个来源对同一事实给出矛盾信息，全部提取并在行末标注 [冲突]
  
  搜索结果：
  {search_results_text}
  
  请输出结构化声明表：

输出格式要求（严格解析）：
  1 | Elon Musk is CEO of X (formerly Twitter) as of 2026 | "Elon Musk has been the CEO of X (formerly Twitter) since..." | https://...
  2 | X has 500M monthly active users | "X reports 500 million monthly active users" | https://...
  3 | X has 450M monthly active users | "X currently has 450 million monthly active users" | https://... [冲突]
  ...

代码实现要点：
  - 用正则解析输出，提取每个声明的四个字段
  - 验证每条声明同时包含 URL 和原文片段
  - 解析失败需要重试（最多 3 次）
  - 冲突检测：对同一主题的多个声明做聚合，标注 [冲突] 的条目保留供生成阶段处理
```

**产物文件：** `outputs/seve/structured_tables.json`

**此步骤的作用：**
- 将"理解搜索结果"和"生成回答"解耦
- 把非结构化网页文本变成可追溯的结构化记录
- 每一条声明都有来源锚定，防止生成时无中生有

---

#### Step 4.2：校准生成

```
输入：
  - Step 4.1 产出的结构化知识表
  - 用户原始问题

Prompt 模板（校准生成）：

  你是一个问答助手。请基于以下结构化知识表回答用户问题。
  
  规则：
  1. 每个事实声明后标注引用编号，如 [1][2]
  2. 如果知识表中标注了 [冲突]，需要同时呈现多方说法并注明来源差异
  3. 如果知识表中没有足够信息，写明"当前信息不足以确定"
  4. 对于只有单一来源的信息，标注"据[来源名称]"
  5. 不要编造知识表中不存在的信息
  
  结构化知识表：
  {structured_table_text}
  
  用户问题：{question}
  
  请回答：

代码实现要点：
  - 解析生成文本中的引用编号
  - 建立 {引用编号 → 结构化知识表中的行} 映射
  - 保存映射供后续反向验证使用
```

**产物文件：** `outputs/seve/generated_answers.json`（含引用映射）

---

#### Step 4.3：反向验证

```
输入：
  - 生成回答中的每一条声明
  - 该声明对应的"引用原文"（从结构化知识表回溯到 search_cache）

Prompt 模板（反向验证）：

  请判断以下引用原文是否包含生成声明中声称的信息。
  
  生成声明：{claim}
  引用原文：{original_text}
  
  仅回答一个词：
  YES（原文明确包含该信息）
  PARTIAL（原文部分支撑但细节不一致）
  NO（原文不包含该信息或与之矛盾）
  
  你的判断：

代码实现要点：
  - 对每条带引用的声明独立做一次验证调用
  - 验证为 NO 的声明 → 删除
  - 验证为 PARTIAL 的声明 → 保留但标注 [部分支撑]
  - 汇总所有验证结果
```

**产物文件：** `outputs/seve/verification_results.json`
**产物文件：** `outputs/seve/final_answers.json`（删除 NO 声明后的最终答案）

**此步骤的作用：**
- 对"生成幻觉"做最终拦截
- 成本低（每个声明只做一次 Y/N 判断，不生成新文本）
- 直接抓出"引用了一篇文档但编造了该文档不存在的信息"这类幻觉

---

### 阶段 5：消融实验

> **说明：** 原方案中 "SEVE w/o 结构化抽取 & 反向验证" 消融条件与 Vanilla RAG（带引用指令）完全等价，已用 Vanilla RAG 替代该位置。两个核心模块的联合贡献通过 `SEVE_full vs Vanilla RAG` 衡量。

#### 5.1 SEVE w/o 反向验证（移除 Step 4）

```
流程：
  Step A: 保留结构化抽取
  Step B: 保留校准生成
  Step C: 不做反向验证，直接输出

目的：衡量反向验证对整体效果的贡献

△贡献 = SEVE_full 得分 - SEVE_wo_rev_verification 得分
```

**产物文件：** `outputs/ablation_wo_rev_verify/answers.json`

---

### 阶段 5.5：实验运行参数与边界处理

#### 5.5.1 统一超参数设定

所有方法统一使用以下参数，确保公平对比：

| 参数 | 设定值 | 说明 |
|------|--------|------|
| Gemini 生成 temperature | **0.0** | 所有生成环节使用贪婪解码，消除随机性 |
| Gemini 验证 temperature | **0.0** | 反向验证使用确定性输出 |
| 搜索结果 Top-K | **5** | 每个问题缓存 5 条搜索结果 |
| 最大输入 token 数 | **32768** | 超出时从第 5 条起向前截断 |
| 最大输出 token 数 | **4096** | 生成回答的最大长度 |
| 解析重试次数 | **3** | 结构化抽取输出解析失败时最多重试 |
| API 超时时间 | **60s** | 单次调用超时上限 |

#### 5.5.2 空搜索结果处理

Bing Search API 可能对某些问题返回 0 条结果或结果全文为空。处理策略：

```
1. 如果 search_cache 中某问题的 results 为空列表：
   → 所有方法直接使用模型内嵌知识生成，不附加任何搜索上下文
   → 在 per_question_details.csv 中标记 search_available=false
   
2. 如果个别结果的 full_text 为空或超短（<50 字符）：
   → 仅使用 title + snippet 替代 full_text
   → 标记该条结果为 truncated

3. 分析时分层报告：
   - 有搜索结果子集的各项指标
   - 无搜索结果子集的各项指标
   - 总体指标
```

#### 5.5.3 统计显著性检验

所有指标对比均需报告统计显著性，防止随机波动被误读为方法差异：

```
方法：配对 bootstrap test（10000 次重采样，α=0.05）
报告：每个指标的点估计 + 95% 置信区间
对比：SEVE vs 每个 baseline 的 p 值
      SEVE_full vs 每个消融变体的 p 值
实现：使用 scipy.stats 或专门的开源工具包
```

---

### 阶段 6：自动评估

**所有方法均需经过以下自动评估：**

#### 6.1 FactScore 评估

```
工具：FActScore (Min et al., 2023)
流程：
  1. 将每个回答原子化（拆分为独立声明）
  2. 用 NLI 模型判断每个声明是否正确
  3. 计算：正确声明数 / 总声明数
```

#### 6.2 幻觉率评估

```
工具：SelfCheckGPT (Manakul et al., 2023) — 使用 NLI 变体
具体方案：不使用原始 SelfCheckGPT 的多轮采样一致性检测（因 Gemini API
         单次调用设定不适合多轮采样），改用其 NLI-based 变体：
  1. 将生成回答按句子拆分
  2. 对每个句子，用开源 NLI 模型（google/t5_xxl_true_nli_mixture）
     判断其是否能从搜索结果文本中推理得出
  3. 计算：不能从搜索结果推理得出的句子数 / 总句子数
```

#### 6.3 引用精度评估

```
工具：开源 NLI 模型（如 google/t5_xxl_true_nli_mixture）独立判定
重要：评估所用的 NLI 模型与 SEVE 方法中的反向验证 Prompt 完全解耦，避免循环论证。
流程：
  1. 提取每个回答中的引用标注
  2. 用独立的开源 NLI 模型判断每个引用是否真实支撑其对应的声明
  3. 计算：正确引用数 / 总引用数
```

#### 6.4 拒答正确率

```
工具：FreshQA 标注的 is_answerable 字段
流程：
  1. 检查模型是否对"不可回答"问题明确表示信息不足
  2. 计算：正确拒答数 / 应拒答问题总数
```

#### 6.5 答案准确率（基于参考答案）

```
工具：LLM-as-Judge（Gemini 3.0 Flash Preview）或开源 NLI 模型
流程：
  1. 将 FreshQA 的 reference_answer 原子化为独立事实点
  2. 判断生成回答是否包含/一致于每个参考事实点
  3. 计算：匹配的参考事实数 / 参考事实总数
说明：此指标弥补仅依赖搜索结果评估的盲区——搜索结果本身可能错误/过时，
     此时需要参考答案作为 ground truth。
```

#### 6.6 答案召回率（Answer Recall）

```
工具：基于 reference_answer 原子化后比对
流程：
  1. 将 reference_answer 拆分为关键信息点 {k1, k2, ..., kn}
  2. 检查生成回答中覆盖了哪些信息点
  3. 计算：被覆盖的信息点数 / 总信息点数
说明：此指标防止"什么都不说所以幻觉率为0"的退化情况，确保方法在
     抑制幻觉的同时不牺牲信息充分性。
```

---

### 阶段 7：人工评估（可选，强烈建议做）

**从 FreshQA 中随机抽 100 题做人工评估：**

| 维度 | 1-5 Likert 量表 |
|------|-----------------|
| Factuality（事实准确性） | 1=完全错误, 5=完全正确 |
| Informativeness（信息充分性） | 1=毫无信息, 5=信息充分 |
| Citation Quality（引用质量） | 1=引用完全错误, 5=引用精准对应原文 |
| Overall（整体有用性） | 1=完全无用, 5=非常有用 |

---

## 四、代码项目结构

```
WeSearch_Experiment/
├── data/
│   ├── freshqa_questions.json      # 原始问题集
│   └── search_cache.json           # 预缓存的 Bing 搜索结果（核心！）
├── src/
│   ├── config.py                   # API 密钥、endpoint 配置
│   ├── bing_client.py              # Bing Search API 封装
│   ├── model_client.py             # Gemini 3.0 Flash Preview 调用封装
│   ├── cache_builder.py            # 阶段2：搜索结果预缓存脚本
│   ├── vanilla_rag.py              # 阶段3.1：Vanilla RAG
│   ├── self_rag.py                 # 阶段3.2：Self-RAG
│   ├── seve.py                     # 阶段4：SEVE 完整流程
│   ├── ablation.py                 # 阶段5：消融实验
│   ├── evaluator.py                # 阶段6：自动评估
│   └── utils.py                    # 公共工具（解析、格式化、重试）
├── outputs/
│   ├── vanilla_rag/
│   │   └── answers.json
│   ├── self_rag/
│   │   └── answers.json
│   ├── seve/
│   │   ├── structured_tables.json
│   │   ├── generated_answers.json
│   │   ├── verification_results.json
│   │   └── final_answers.json
│   ├── ablation_wo_rev_verify/
│   │   └── answers.json
│   └── evaluation/
│       ├── results_summary.csv     # 各方法各指标汇总
│       └── per_question_details.csv
├── human_eval/
│   ├── sampled_100_questions.json
│   └── evaluation_rubric.md
└── README.md
```

---

## 五、论文写作指引

### 5.1 题目建议

**中文：** 基于结构化抽取与反向验证的联网大模型幻觉缓解方法

**英文：** Mitigating Web-Connected Hallucination in LLMs via Structured Extraction and Reverse Verification

### 5.2 论文创新点表述（三段式，这是论文的灵魂）

**创新点 1：中间结构化表示解耦检索与生成**

> 现有 RAG 方法直接在非结构化检索文本上生成，导致注意力稀释和跨段落合成幻觉。SEVE 在检索和生成之间插入结构化知识表作为中间表示，将"事实理解"与"语言表达"解耦，生成阶段只看到已锚定来源的声明条目，显著降低了编造信息的自由度。

**创新点 2：轻量级声明-原文一对一验证闭环**

> 与 CoVe（需生成验证问题再做检索，形成"生成-检索-再生成"循环）和 CRITIC（需调用外部工具如计算器、搜索引擎做验证）不同，SEVE 的反向验证仅做声明与原文的一对一字符串匹配判断（YES/PARTIAL/NO），不生成新文本，不触发新检索。更重要的是，结构化知识表在抽取阶段已建立了"声明→原文片段→URL"的映射，验证时无需反复搜索和匹配——这是一条 CoVe 和 CRITIC 都不具备的捷径。该步骤成本极低但能有效拦截引用捏造和搜索结果误读两类高频幻觉。

**创新点 3：低成本、不依赖训练的 API 级别方案**

> SEVE 完全基于 API 调用实现，不需要模型微调、RLHF 或架构修改，对基座模型透明。这使其可快速适配到任意搜索工具和模型组合上。

### 5.3 论文各章节写作要点

**第 1 章：引言**
- 以联网幻觉的具体案例开头（如"模型在引用了某网页后却编造了该网页不存在的数据"），制造冲击感
- 提出核心问题：为什么模型在"手里有正确答案"时仍然出错？
- 概述本文贡献（对应上述三个创新点）

**第 2 章：相关工作**
- 2.1 大模型幻觉（Huang et al., 2023; Zhang et al., 2024; Ji et al., 2023）
- 2.2 检索增强生成（Lewis et al., 2020; Self-RAG; REPLUG; FreshLLM）
- 2.3 幻觉缓解方法（CoVe; CRITIC; SAFE; RARR; FActScore）
  - 重点讨论 RARR（归因+修正循环）与本文反向验证的异同：RARR 需要额外的
    检索和编辑步骤，SEVE 仅在已有搜索结果上做判断，不产生新检索
  - 讨论 SAFE（Search-Augmented Factuality Evaluator）与本文的区别：
    SAFE 依赖搜索引擎增强评估，SEVE 的验证直接基于已缓存的搜索结果
- 2.4 事实验证与归因（SelfCheckGPT; HaluEval; Bohnet et al., 2022 Attributed QA）
  - 讨论归因式问答（Attributed QA）与本文引用机制的关联和差异

**第 3 章：方法**
- 3.1 问题定义
- 3.2 总体框架图
- 3.3 结构化抽取（Prompt 设计 + 输出格式 + 设计理由）
- 3.4 校准生成（引用强制 + 推测降级 + 信息不足处理）
- 3.5 反向验证（操作定义 + 三种判定结果的处理策略）

**第 4 章：实验设置**
- 4.1 数据集：FreshQA
- 4.2 基座模型：Gemini 3.0 Flash Preview
- 4.3 搜索工具：Bing Search API
- 4.4 搜索结果缓存策略与公平性保证
- 4.5 Baseline 方法：Vanilla RAG、Self-RAG
- 4.6 评估指标：FactScore、幻觉率、引用精度、答案准确率、答案召回率、拒答正确率
- 4.7 消融实验：移除反向验证（以 Vanilla RAG 为 full-removal 参照）

**第 5 章：结果与分析**
- 5.1 主实验结果表
- 5.2 消融实验结果
- 5.3 错误分析（幻觉类型分布的变化）
- 5.4 案例分析（选 2-3 个典型例子，展示 SEVE 如何纠正幻觉）

**第 6 章：讨论**
- 6.1 结构化抽取的质量稳定性
- 6.2 反向验证的局限性（PARTIAL 判定的模糊地带）
- 6.3 成本分析（SEVE 相比 baseline 额外调用了多少次 API）
- 6.4 计算预算公平性：Vanilla RAG 仅需 1 次生成调用而 SEVE 需 2 次生成 + ~5 次验证调用，
     以 Vanilla RAG 作为消融参照（等同于同时移除抽取和验证），可与 SEVE w/o 反向验证
     形成完整消融链：Vanilla RAG → +抽取 → +验证 = SEVE full
- 6.5 Bing Search API 搜索质量的边界效应

**第 7 章：结论**

### 5.4 预测结果表格模板

**主实验结果（用实际数据填充）：**

| 方法 | FactScore ↑ | Hallucination Rate ↓ | Citation Precision ↑ | Answer Accuracy ↑ | Answer Recall ↑ | Refusal Acc ↑ |
|------|-------------|---------------------|-----------------|------------------|-----------------|---------------|
| Vanilla RAG | ? | ? | ? | ? | ? | ? |
| Self-RAG | ? | ? | ? | ? | ? | ? |
| **SEVE (ours)** | ? | ? | ? | ? | ? | ? |

**消融实验结果：**

| 变体 | FactScore | Citation Precision | Answer Accuracy | Δ vs Full SEVE |
|------|-----------|-------------------|----------------|----------------|
| SEVE (full) | ? | ? | ? | — |
| Vanilla RAG (= w/o 抽取 & 验证) | ? | ? | ? | ? |
| SEVE w/o 反向验证 | ? | ? | ? | ? |

---

## 六、参考文献（关键文献清单）

```
[1] Lewis, P., et al. (2020). Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks. NeurIPS.
[2] Asai, A., et al. (2023). Self-RAG: Learning to Retrieve, Generate, and Critique through Self-Reflection. ICLR 2024.
[3] Dhuliawala, S., et al. (2024). Chain-of-Verification Reduces Hallucination in Large Language Models. ACL 2024.
[4] Vu, T., et al. (2023). FreshLLMs: Refreshing Large Language Models with Search Engine Augmentation. arXiv.
[5] Gao, T., et al. (2023). ALCE: Enabling Large Language Models to Generate Text with Citations. EMNLP 2023.
[6] Min, S., et al. (2023). FActScore: Fine-grained Atomic Evaluation of Factual Precision. EMNLP 2023.
[7] Manakul, P., et al. (2023). SelfCheckGPT: Zero-Resource Black-Box Hallucination Detection. EMNLP 2023.
[8] Ji, Z., et al. (2023). Survey of Hallucination in Natural Language Generation. ACM Computing Surveys.
[9] Huang, L., et al. (2023). A Survey on Hallucination in Large Language Models. arXiv.
[10] Zhang, Y., et al. (2024). Siren's Song in the LLM Ocean: A Survey on Hallucination. arXiv.
[11] Gou, Z., et al. (2024). CRITIC: LLMs Can Self-Correct with Tool-Interactive Critiquing. ICLR 2024.
[12] Wei, J., et al. (2024). SAFE: Search-Augmented Factuality Evaluator. arXiv.
[13] Yin, Z., et al. (2023). Do Large Language Models Know What They Don't Know? ACL Findings 2023.
[14] Li, J., et al. (2023). HaluEval: A Large-Scale Hallucination Evaluation Benchmark. EMNLP 2023.
[15] Liu, N. F., et al. (2023). Lost in the Middle: How Language Models Use Long Contexts. TACL.
[16] Mallen, A., et al. (2023). When Not to Trust Language Models. ACL 2023.
[17] Shi, W., et al. (2023). REPLUG: Retrieval-Augmented Black-Box Language Models. arXiv.
[18] Peng, B., et al. (2023). Check Your Facts and Try Again. arXiv.
[19] Ram, O., et al. (2023). In-Context Retrieval-Augmented Language Models. TACL.
[20] Wang, C., et al. (2024). RARR: Researching and Revising What Language Models Say. ACL 2024.
[21] Bohnet, B., et al. (2022). Attributed Question Answering: Evaluation and Modeling for Attributed Large Language Models. arXiv.
[22] Mialon, G., et al. (2024). GAIA: A Benchmark for General AI Assistants. ICLR 2024.
```

---

## 七、实验步骤 check list（按顺序执行）

```
□ 1. 下载 FreshQA 数据集，转为统一 JSON 格式
□ 2. 配置 Bing Search API 和 Gemini 3.0 Flash Preview API 的调用代码
□ 3. 运行 cache_builder.py：对所有问题调用 Bing Search API，生成 search_cache.json
    （注意处理空搜索结果，按 5.5.2 策略标记）
□ 4. 运行 vanilla_rag.py：Vanilla RAG 方法（temperature=0.0），输出到 outputs/vanilla_rag/
□ 5. 运行 self_rag.py：Self-RAG 方法（3 轮，temperature=0.0），输出到 outputs/self_rag/
□ 6. 运行 seve.py：完整 SEVE 流程（结构化抽取 + 校准生成 + 反向验证）
□ 7. 运行 ablation.py：消融变体（wo_rev_verify：仅移除反向验证）
    （Vanilla RAG 作为移除全部两模块的参照，无需单独运行）
□ 8. 运行 evaluator.py：自动评估所有方法的六项指标
    （FactScore / 幻觉率 / 引用精度 / 答案准确率 / 答案召回率 / 拒答正确率）
    引用精度评估使用独立开源 NLI 模型，与 SEVE 方法解耦
□ 9. 运行统计检验：配对 bootstrap test，生成 95% 置信区间和 p 值
□ 10. 汇总结果，填写主实验表和消融实验表
□ 11. 按 requires_fresh 字段做分层分析（快变/慢变/不变问题的效果差异）
□ 12. 从 FreshQA 中抽样 100 题，完成人工评估
□ 13. 撰写论文
```

---

## 八、API 调用量估算

| 步骤 | 每题的调用次数 | 600 题总计 |
|------|---------------|-----------|
| Bing Search API（仅一次，缓存后复用） | 1 次搜索 | 600 次搜索 |
| Vanilla RAG 生成 | 1 次生成 | 600 次 |
| Self-RAG（3 轮） | 3 次生成 | 1800 次 |
| SEVE 结构化抽取 | 1 次生成 | 600 次 |
| SEVE 校准生成 | 1 次生成 | 600 次 |
| SEVE 反向验证 | ~5 次判断/题 | ~3000 次 |
| 消融 wo 反向验证 | 1 次抽取 + 1 次生成 | 1200 次 |
| 自动评估（NLI 模型） | ~10 次/题 | ~6000 次 |
| **总计（含评估）** | | **~15600 次调用** |

注意：评估阶段可使用更轻量/免费的 NLI 模型（如 `google/t5_xxl_true_nli_mixture`）本地运行，不消耗 API 配额。
