# 开题报告 PPT 大纲（10 页）

---

## 第 1 页：封面

**基于结构化抽取与反向验证的联网大模型幻觉缓解方法**

姓名 /专业 / 日期

---

## 第 2 页：问题背景

- 大模型联网搜索已成标配，但联网幻觉严重制约可信度
- 核心矛盾：搜索已返回正确信息，但模型在非结构化网页文本上直接生成时注意力稀释，最终仍出错
- 三类典型幻觉：引用捏造、检索结果误读、参数知识覆盖搜索结果

**核心问题：** 如何强制模型忠实反映检索信息而非偏离？

---

## 第 3 页：联网幻觉的成因

### 四层根因分析

| 层级 | 原因 | 具体表现 |
|------|------|---------|
| **检索层** | 搜索结果质量参差 | 非权威来源、过期信息混入、广告/营销内容干扰 |
| **注意力层** | 长上下文注意力稀释 | "Lost in the Middle" — 关键信息位于上下文中间时识别率显著下降 |
| **生成层** | 参数知识与检索冲突 | 预训练记忆覆盖检索结果，模型"相信"自己记住的多于搜索返回的 |
| **合成层** | 跨文档推理失败 | 多个正确的零散事实被错误拼接，合成出原文不存在的虚假结论 |

### 最危险的幻觉：引用捏造

> 模型引用了一个真实存在的 URL，却编造了该页面不存在的信息 —— 用户看到引用往往不再怀疑，危害最大

### 根本矛盾

> 模型被训练为"相信上下文"，但搜索结果作为上下文质量不受控 → 直接信任原始搜索结果 = 系统性风险

---

## 第 4 页：SEVE 方法总览

```
                        ┌─────────────┐
                        │  用户问题    │
                        └──────┬──────┘
                               │
                               ▼
┌──────────────────────────────────────────────────────────────┐
│  Step 1: WeSearch 检索                                       │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │  输入: 用户原始问题                                       │ │
│  │  调用: WeSearch API（腾讯垂类搜索）                       │ │
│  │  输出: Top-5 网页原文（标题 + URL + 全文 + 发布日期）     │ │
│  │  存入: search_cache.json（所有方法共享）                  │ │
│  └─────────────────────────────────────────────────────────┘ │
└──────────────────────────┬───────────────────────────────────┘
                           │ 5 篇网页原文
                           ▼
┌──────────────────────────────────────────────────────────────┐
│  Step 2: 结构化抽取                                          │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │  Prompt: "你是事实抽取器，只抽取、不推理、不总结"         │ │
│  │  输入: 5 篇网页原文                                       │ │
│  │  模型: Gemini 3.0 Flash Preview                          │ │
│  │  输出: 结构化声明表                                       │ │
│  │  ┌──────┬──────────────────┬──────────┬──────────┐      │ │
│  │  │ ID   │ 声明内容          │ 原文片段  │ 源URL    │      │ │
│  │  ├──────┼──────────────────┼──────────┼──────────┤      │ │
│  │  │ 1    │ X 的 CEO 是...    │ "..."    │ url_1    │      │ │
│  │  │ 2    │ X 月活 5 亿       │ "..."    │ url_2    │      │ │
│  │  └──────┴──────────────────┴──────────┴──────────┘      │ │
│  │  解析: 正则提取 + 格式校验 + 失败重试(最多3次)            │ │
│  └─────────────────────────────────────────────────────────┘ │
└──────────────────────────┬───────────────────────────────────┘
                           │ 结构化知识表
                           ▼
┌──────────────────────────────────────────────────────────────┐
│  Step 3: 校准生成                                            │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │  输入: 结构化知识表 + 用户问题                            │ │
│  │  规则:                                                   │ │
│  │    · 每句事实声明后标注引用编号 [1][2]                    │ │
│  │    · 知识表无支撑的声明 → 降级为 "[推测]"                 │ │
│  │    · 知识表信息不足 → 主动标注 "[信息不足]"               │ │
│  │    · 禁止编造知识表外信息                                  │ │
│  │  输出: 带引用的回答 + {引用编号 → 源声明} 映射表          │ │
│  └─────────────────────────────────────────────────────────┘ │
└──────────────────────────┬───────────────────────────────────┘
                           │ 带引用的回答 + 映射表
                           ▼
┌──────────────────────────────────────────────────────────────┐
│  Step 4: 反向验证                                            │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │  对每条带引用的声明，独立执行:                             │ │
│  │                                                          │ │
│  │  Prompt: "原文是否包含此声明？仅回答 YES/PARTIAL/NO"      │ │
│  │  ┌─────────────────────────────────────────────────┐    │ │
│  │  │  生成声明: "X 的 CEO 是 Elon Musk"  [引用 url_1] │    │ │
│  │  │  原文片段: "Elon Musk leads X as CEO..."        │    │ │
│  │  │  判定: YES ✓ → 保留                              │    │ │
│  │  └─────────────────────────────────────────────────┘    │ │
│  │  ┌─────────────────────────────────────────────────┐    │ │
│  │  │  生成声明: "X 市值 5000 亿美元"  [引用 url_1]     │    │ │
│  │  │  原文片段: (不包含任何市值信息)                    │    │ │
│  │  │  判定: NO ✗ → 删除                               │    │ │
│  │  └─────────────────────────────────────────────────┘    │ │
│  │                                                          │ │
│  │  处理: YES→保留 / PARTIAL→保留+标注[部分支撑] / NO→删除  │ │
│  └─────────────────────────────────────────────────────────┘ │
└──────────────────────────┬───────────────────────────────────┘
                           │
                           ▼
┌──────────────────────────────────────────────────────────────┐
│                        最终输出                               │
│  带引用标注 + 验证状态 + 置信度 + [推测]/[信息不足] 标记     │
└──────────────────────────────────────────────────────────────┘
```

---

## 第 5 页：两个核心组件

### 结构化抽取

| 要点 | 说明 |
|------|------|
| 输入 | WeSearch Top-5 网页原文 |
| Prompt 约束 | "只抽取原文明确陈述的事实，不推断、不总结" |
| 输出格式 | `ID \| 声明 \| 原文片段 \| URL \| 证据类型` |
| 解析 | 正则 + 校验 + 最长重试 3 次 |
| 作用 | 将非结构化网页 → 可追溯的结构化记录，切断凭空编造路径 |

### 反向验证

| 要点 | 说明 |
|------|------|
| 输入 | 每条声明 + 其引用对应的原文片段 |
| Prompt | "原文是否包含此声明？仅回答 YES/PARTIAL/NO" |
| 处置 | NO→删除 / PARTIAL→保留+标注 / YES→保留 |
| 特点 | 不生成新文本、不调用新搜索、逐条 Y/N 极低成本 |
| 作用 | 精准拦截"引用了某文档但编造了该文档不存在的信息" |

---

## 第 6 页：实验设计

### 公平性保证

> 所有方法共享同一份 `search_cache.json`，同一问题 → 同一搜索结果，锁定搜索变量，唯一变量 = 生成策略。

### 实验设定

| 项目 | 取值 |
|------|------|
| 数据集 | FreshQA（600 题） |
| 基座模型 | Gemini 3.0 Flash Preview（全部环节统一） |
| 搜索工具 | WeSearch |
| Baseline | Vanilla RAG / Self-RAG |
| 本文 | SEVE（结构化抽取 + 校准生成 + 反向验证） |
| 消融 | w/o 结构化抽取 / w/o 反向验证 |

### 评估指标

| 指标 | 工具 |
|------|------|
| FactScore（原子事实正确率） | FActScore (Min 2023) |
| Hallucination Rate（幻觉占比） | SelfCheckGPT (Manakul 2023) |
| Citation Precision（引用准确性） | ALCE (Gao 2023) |
| Refusal Accuracy（拒答正确率） | FreshQA 标注 |

---

## 第 7 页：方法对比矩阵

| 环节 | Vanilla RAG | Self-RAG | SEVE (ours) |
|------|:-----------:|:--------:|:-----------:|
| 搜索结果来源 | search_cache | search_cache | search_cache |
| 结构化中间表示 | ✗ | ✗ | ✓ |
| 反思/自我校验 | ✗ | ✓（3 轮） | ✗ |
| 校准生成（引用强制） | ✗ | ✗ | ✓ |
| 反向验证 | ✗ | ✗ | ✓ |
| API 调用/题 | 1 次 | 3 次 | 2 + N 次 |
| 可追溯性 | 无 | 无 | 每声明→URL |

> N = 生成回答中的声明数，通常 3~8，每次仅做 Y/N 判断（短 prompt，低成本）

---

## 第 8 页：消融实验设计

**目的：** 量化结构化抽取与反向验证各自对整体效果的独立贡献。

| 变体 | 结构化抽取 | 校准生成 | 反向验证 | 贡献计算 |
|------|:---------:|:-------:|:-------:|---------|
| SEVE (full) | ✓ | ✓ | ✓ | — |
| w/o 结构化抽取 | ✗ | ✗ | ✓ | Δ₁ = Full − 本行 |
| w/o 反向验证 | ✓ | ✓ | ✗ | Δ₂ = Full − 本行 |

**消融 w/o 结构化抽取**：直接基于原始搜索结果生成带引用回答 → 仍做反向验证

**消融 w/o 反向验证**：保留结构化抽取 + 校准生成 → 跳过验证直接输出

---

## 第 9 页：创新点与贡献

**1. 中间结构化表示**
- 非结构化网页 → 结构化事实表，解耦"理解"与"表达"
- 每条声明可追溯到具体 URL 和原文片段
- 生成阶段看到的是锚定来源的声明条目而非散乱网页文本

**2. 轻量级反向验证**
- 不做新搜索、不生成新文本，逐声明 Y/N 判断
- 与 CoVe（需生成验证问题）、CRITIC（需外部工具）形成差异化
- 精准拦截引用捏造 —— 联网幻觉中危害最高的类型

**3. 公平对比框架**
- 所有方法共享 WeSearch 缓存，锁定搜索变量
- 仅衡量生成策略差异
- 对基座模型透明，可适配任意搜索工具 + 模型组合

---

## 第 10 页：感谢聆听

